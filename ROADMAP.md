# 実装ロードマップ

TCP/IP を理解することを目的とした、スモールステップの実装順序。

## 進捗

- [x] Step 1: hexdump とバイトオーダー変換
- [x] Step 2: インターネットチェックサム
- [x] Step 3: TAP デバイスを開いて生フレームを dump
- [x] Step 4: Ethernet ヘッダのパース
- [x] Step 5: Ethernet フレームの組み立てと送信
- [x] Step 6: ARP パケットのパース
- [x] Step 7: ARP 応答を返す 🏁
- [x] Step 8: ARP テーブルと ARP 要求
- [x] Step 9: IPv4 ヘッダのパース + チェックサム検証
- [x] Step 10: IPv4 ヘッダの組み立てと送信
- [x] Step 11: ICMP Echo のパース
- [x] Step 12: Echo Reply を返す 🏁
- [x] Step 13: UDP ヘッダと疑似ヘッダチェックサム
- [ ] Step 14: UDP echo サーバ 🏁
- [ ] Step 15: ポート多重化（ソケットテーブル）
- [ ] Step 16: TCP ヘッダのパース
- [ ] Step 17: RST 応答（CLOSED の振る舞い）
- [ ] Step 18: 状態機械を純粋関数として実装
- [ ] Step 19: 3-way handshake 🏁
- [ ] Step 20: データ受信と ACK
- [ ] Step 21: データ送信と再送
- [ ] Step 22: クローズ処理と TIME_WAIT
- [ ] Step 23: ウィンドウとフロー制御

## この順序にした理由

- **チェックサムが最初**: IP / ICMP / UDP / TCP すべてが同じアルゴリズムを使う。
- **ARP が IP より先**: 「L3 アドレスから L2 アドレスを引く」という層の依存が最小の形で現れ、1 往復で完結するので「応答を返す」体験が最初にできる。
- **IP はパーサだけ**: 単体では動作確認できない。ICMP で初めて往復する — 「L3 は単体では意味を持たない」こと自体が理解の一部。
- **疑似ヘッダは UDP で学ぶ**: L4 チェックサムがなぜ L3 アドレスを含むのかを、TCP の複雑さに入る前に押さえる。
- **TCP は RST から**: CLOSED の規則は状態機械で最小、かつ即座に動作確認できる。「接続を張る」前に「接続を断る」。
- **状態機械を先に、I/O は後**: TCP の難しさの本質は状態遷移であって I/O ではない。純粋関数ならソケットも TAP も無しで全遷移をテストできる。

---

## Phase 0 — 土台（ネットワーク不要）

### Step 1: hexdump とバイトオーダー変換

- **実装**: バイト列を `tcpdump -x` 風に 16 進ダンプする関数。ビッグエンディアンの u16 / u32 を読み書きするヘルパ。
- **テスト**: 既知のバイト列 → 期待する文字列。
- **理解のポイント**: なぜネットワークバイトオーダーはビッグエンディアンなのか。x86 / ARM はリトルエンディアンなので、読み書きのたびに変換が要る。

### Step 2: インターネットチェックサム（RFC 1071）

- **実装**: 16bit ワードの 1 の補数和の 1 の補数。奇数長のパディングを忘れずに。
- **テスト**: 実際の IPv4 ヘッダで「チェックサムフィールドを 0 にして計算した結果 == 元の値」。
- **理解のポイント**: なぜ 1 の補数和なのか（エンディアン非依存、部分和の再利用）。「フィールドを含めたまま計算すると 0 になる」性質はなぜ成り立つか。

---

## Phase 1 — パケットを目にする

### Step 3: TAP デバイスを開いて生フレームを dump

- **実装**: `/dev/net/tun` を open し、`TUNSETIFF` ioctl で `IFF_TAP | IFF_NO_PI` を指定して `tap0` に紐付け、read したバイト列を dump する。
- **ハマりどころ**: TUN 系の定数は std に無いので自分で定義する。`IFF_NO_PI` を付けないと先頭に 4 バイトの情報ヘッダが付いてズレる。
- **テスト**: スタック起動中に `ping 192.168.70.2` で ARP 要求が流れてくる。`tcpdump -i tap0 -xx` と自分の dump の一致を確認。
- **理解のポイント**: TAP から読めるのは Ethernet フレームそのもので、プリアンブルも FCS も含まれない。カーネルがどこまでやってくれているか。

---

## Phase 2 — Ethernet (L2)

### Step 4: Ethernet ヘッダのパース

- **実装**: 先頭 14 バイトを宛先 MAC / 送信元 MAC / EtherType に分解する純粋関数。ペイロードは入力スライスへの参照。main に「EtherType で分岐」の骨組み。
- **テスト**: 実フレームで EtherType が `0x0806` (ARP)。短すぎる入力はエラー。
- **理解のポイント**: EtherType による上位層の多重分離。ブロードキャスト MAC の意味。実 NIC のプロミスキャスモードと TAP の違い。

### Step 5: Ethernet フレームの組み立てと送信

- **実装**: フレームを組み立てて TAP に write。スタック自身の MAC をここで定義。
- **テスト**: 送出したフレームが `tcpdump -i tap0` に現れる（中身は不正でよい）。
- **理解のポイント**: 自分の MAC はなぜ `02:` で始めるのか（ローカル管理ビット）。最小フレーム長 60 バイトのパディングは誰がやるのか。

---

## Phase 3 — ARP: 最初の往復

### Step 6: ARP パケットのパース

- **実装**: ARP 本体 28 バイトの分解。
- **テスト**: 実際の ARP 要求バイト列でフィールドを検証。
- **理解のポイント**: アドレス長フィールドがあるのは、ARP が Ethernet + IPv4 専用ではなく汎用のアドレス解決プロトコルとして設計されているから。

### Step 7: ARP 応答を返す 🏁 **マイルストーン 1**

- **実装**: 自分の IP 宛の ARP 要求に、オペコードを reply にして送信元・宛先を入れ替えた応答を返す。
- **テスト**: `ip neigh flush all && ping -c1 192.168.70.2` の後、`ip neigh` にスタックの MAC が `REACHABLE` で現れる。
- **理解のポイント**: ping はまだ通らないのに ARP の会話は成立している — 層が独立して動いている。

### Step 8: ARP テーブルと ARP 要求

- **実装**: IP → MAC のキャッシュ（固定長配列で十分）。受信 ARP から学習。自分から ARP 要求をブロードキャストする関数。
- **テスト**: 挿入・検索・上書きをユニットテストで。実機では `192.168.70.1` への要求にカーネルが応答し、テーブルに載る。
- **理解のポイント**: ARP 要求の「宛先 MAC」は 2 箇所ある（Ethernet ヘッダ = ブロードキャスト、ARP 本体の target = 0 埋め）。エントリの有効期限は必要か。

---

## Phase 4 — IPv4 (L3)

### Step 9: IPv4 ヘッダのパース + チェックサム検証

- **実装**: 全フィールドのパース。チェックサム検証、宛先が自分でないものは破棄。
- **テスト**: 実際の ICMP echo request でプロトコル番号 1、検証が通る。1 バイト壊すと落ちる。
- **理解のポイント**: IHL が 32bit ワード数なのはなぜか。チェックサムがヘッダのみなのはなぜか（ルータが TTL を減らすたび再計算するため）。最小フレーム長のパディングを剥がすのは誰か。プロトコル番号の多重分離は EtherType と同じ構造。

### Step 10: IPv4 ヘッダの組み立てと送信

- **実装**: ヘッダ組み立てとチェックサム計算。宛先 MAC の解決もここで行う — ARP テーブル検索 → 未解決なら ARP 要求 → 解決済みなら Ethernet 送信。ICMP も UDP も TCP も IP を経由するので、外に出る経路はここ 1 本になる。
- **テスト**: `tcpdump -i tap0 -vv` で `bad cksum` が出ない（tcpdump はチェックサムを検証してくれる）。解決済み IP は即フレームになり、未解決 IP は ARP 要求が先に出る（送信バッファを注入して確認）。
- **理解のポイント**: TTL の初期値。識別子フィールドの用途（フラグメント再構成）。フラグメントは実装しない。未解決の間のパケットをどうするか（捨てる / 1 個保持 / キュー）。Linux は何個キューイングするか。

---

## Phase 5 — ICMP: ping が通る

### Step 11: ICMP Echo のパース

- **実装**: タイプ / コード / チェックサム / 識別子 / シーケンス番号 + データ。チェックサムはメッセージ全体が対象（疑似ヘッダは無い）。
- **テスト**: 実際の echo request でタイプ 8、検証が通る。
- **理解のポイント**: ICMP はポート番号を持たない（「L4」とは呼ばれない）。識別子は `ping` プロセスが自分宛の応答を識別するためのもの。

### Step 12: Echo Reply を返す 🏁 **マイルストーン 2**

- **実装**: タイプ 8→0、データはそのまま、チェックサム再計算して送り返す。
- **テスト**: `ping -c3 192.168.70.2` がロス 0% で応答を受け取る。
- **理解のポイント**: TAP → Ethernet → ARP → IPv4 → ICMP が全部つながった。1 回の ping でコードのどこを何回通ったか追う。

---

## Phase 6 — UDP: L4 の入り口

### Step 13: UDP ヘッダと疑似ヘッダチェックサム

- **実装**: UDP ヘッダ。チェックサムは**疑似ヘッダ**（送信元 IP・宛先 IP・0・プロトコル番号・UDP 長）+ ヘッダ + データが対象。TCP も同じ疑似ヘッダを使う。
- **ハマりどころ**: IPv4 では UDP チェックサム 0 は「未計算」の意味。カーネルがオフロード前提で未計算のまま流してくることがある（`ethtool -K tap0 tx off` で切り分け）。
- **テスト**: 実パケットで検証が通る。疑似ヘッダの組み立ては別関数にして単独でテスト。
- **理解のポイント**: **なぜ L4 のチェックサムが L3 のアドレスを含むのか** — 誤配送の検知。層の厳密な分離より実用を取った妥協点。

### Step 14: UDP echo サーバ 🏁 **マイルストーン 3**

- **実装**: 特定ポート宛の UDP を、送信元と宛先を入れ替えて返す。
- **テスト**: `echo hello | nc -u 192.168.70.2 7` で `hello` が返る。
- **理解のポイント**: UDP が IP に足したものは実質「ポート番号」だけ。

### Step 15: ポート多重化（ソケットテーブル）

- **実装**: 「ポート番号 → ハンドラ」のテーブル。未使用ポートには ICMP Port Unreachable（余裕があれば）。
- **テスト**: 2 つのポートに別々のハンドラを登録し、振り分けをユニットテストで。
- **理解のポイント**: これが「ソケット」の正体。宛先ポートだけで足りる UDP に対し、TCP は何で識別するか（4 タプル）を先に考えておく。

---

## Phase 7 — TCP

状態機械・順序制御・再送・フロー制御が絡み合う。**一度に全部やらず、必ず段階を分けること。**

### Step 16: TCP ヘッダのパース

- **実装**: 全フィールドのパース。オプションは長さだけ読んで読み飛ばす。
- **テスト**: 実際の SYN パケットで SYN フラグ、疑似ヘッダ込みチェックサム（Step 13 を再利用）。
- **理解のポイント**: データオフセットは可変長オプションのため。ウィンドウフィールドが最初からある — フロー制御は後付けではなく設計の中核。

### Step 17: RST 応答（CLOSED の振る舞い）

- **実装**: コネクションを持たない状態で届いたセグメントに RST を返す。RFC 9293 3.10.7.1 どおり: 受信セグメントに ACK があれば `seq = 受信 ACK 番号`、無ければ `seq = 0, ack = 受信 seq + セグメント長（SYN/FIN は +1）` で RST+ACK。
- **テスト**: `nc 192.168.70.2 9999` が即座に **Connection refused**。`tcpdump -i tap0 -S` で SYN → RST。
- **理解のポイント**: RST にも正しいシーケンス番号が要る理由（盲目的リセット攻撃への防御）。「ポートが閉じている」は UDP では ICMP、TCP では RST。

### Step 18: 状態機械を純粋関数として実装

- **実装**: 状態の enum と `fn transition(state, event) -> (next_state, action)` の純粋関数。I/O は一切書かない。handshake 系の遷移から書き始める。
- **テスト**: **ここが最重要。** RFC 9293 の状態遷移図の全遷移を網羅する（3-way handshake、同時オープン、同時クローズ、RST 受信）。
- **理解のポイント**: CLOSE_WAIT と FIN_WAIT_2 が非対称に見える理由。TIME_WAIT だけ時間で抜ける理由。図を見ずに書けるまで繰り返す。

### Step 19: 3-way handshake（受動オープン） 🏁 **マイルストーン 4**

- **実装**: 4 タプルをキーにした TCB テーブル。LISTEN で SYN → SYN+ACK → ACK で ESTABLISHED。ISN の生成。TCB が無いセグメントは Step 17 の RST へ。
- **テスト**: `nc 192.168.70.2 8080` が確立して待機。9999 は引き続き refused。`tcpdump -S` で 3 パケットを目視。
- **理解のポイント**: なぜ 2-way ではなく 3-way か。ISN をランダムにする理由。SYN に対する ACK が `seq+1` になる理由（SYN はシーケンス空間を 1 消費する）。

### Step 20: データ受信と ACK

- **実装**: 受信バッファ。順序どおりなら受け入れて ACK、重複は破棄、飛んだら（まずは）破棄。
- **テスト**: `nc` から文字列を受信できる。シーケンス比較関数を重点的にユニットテスト。
- **理解のポイント**: シーケンス番号のラップアラウンド。単純な `<` では破綻し、符号付き差分で比較する。必ずバグる場所。

### Step 21: データ送信と再送

- **実装**: 送信バッファと未 ACK データの保持。固定タイムアウトで再送。**イベントループにここで初めて「時間」が入る** — ブロッキング read を poll(timeout) に変える。
- **テスト**: 送ったデータが `nc` に表示される。ACK を無視するテストで再送が起きる。
- **理解のポイント**: 「ACK が返るまでデータを捨てられない」のが信頼性の本質。固定 RTO の問題を体感する（RFC 6298 の RTT 推定への動機）。

### Step 22: クローズ処理と TIME_WAIT

- **実装**: FIN の送受信、状態機械に沿った遷移。TIME_WAIT の 2MSL タイマ。
- **テスト**: `nc` を Ctrl-D で閉じると 4-way でクローズする。
- **理解のポイント**: **なぜ TIME_WAIT が必要か** — 遅延パケットの次コネクションへの混入防止と、最終 ACK 喪失時の再送応答。

### Step 23: ウィンドウとフロー制御

- **実装**: 受信バッファの空きを advertised window として載せる。相手のウィンドウを超えて送らない。ゼロウィンドウのプローブ。
- **テスト**: 受信バッファを消費せずにいると、カーネル側の送信が止まる。
- **理解のポイント**: フロー制御（相手の受信能力）と輻輳制御（ネットワークの容量）は別物。Silly Window Syndrome とは何か。

---

## 発展課題（Phase 7 完了後）

- **能動オープン**（クライアント側）— SYN_SENT の経路。
- **輻輳制御** — スロースタート、輻輳回避、高速再送（RFC 5681）。
- **TCP オプション** — MSS、ウィンドウスケール、SACK、タイムスタンプ。
- **順序が乱れたセグメントの再構成** — Step 20 で破棄していたものを並べ替える。
- **IP フラグメント** — 分割と再構成。
- **ICMP のエラー通知** — Destination Unreachable / Time Exceeded。
- **簡易ソケット API** — `listen` / `accept` / `read` / `write` を自作スタックの上に。
