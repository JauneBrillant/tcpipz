# 実装ロードマップ

TCP/IP を理解することを目的とした、スモールステップの実装順序。

## 進捗

- [x] Step 1: hexdump とバイトオーダー変換
- [x] Step 2: インターネットチェックサム
- [x] Step 3: TAP デバイスを開いて生フレームを dump
- [ ] Step 4: Ethernet ヘッダのパース
- [ ] Step 5: Ethernet フレームの組み立てと送信
- [ ] Step 6: ARP パケットのパース
- [ ] Step 7: ARP 応答を返す 🏁
- [ ] Step 8: ARP テーブルと ARP 要求
- [ ] Step 9: 送信経路の共通化
- [ ] Step 10: IPv4 ヘッダのパース + チェックサム検証
- [ ] Step 11: IPv4 ヘッダの組み立てと送信
- [ ] Step 12: ICMP Echo のパース
- [ ] Step 13: Echo Reply を返す 🏁
- [ ] Step 14: UDP ヘッダと疑似ヘッダチェックサム
- [ ] Step 15: UDP echo サーバ 🏁
- [ ] Step 16: ポート多重化（ソケットテーブル）
- [ ] Step 17: TCP ヘッダのパース
- [ ] Step 18: RST 応答（CLOSED の振る舞い）
- [ ] Step 19: 状態機械を純粋関数として実装
- [ ] Step 20: 3-way handshake 🏁
- [ ] Step 21: データ受信と ACK
- [ ] Step 22: データ送信と再送
- [ ] Step 23: クローズ処理と TIME_WAIT
- [ ] Step 24: ウィンドウとフロー制御

## 各ステップの進め方

1. **最小実装** — そのステップで動作確認できる最小限だけを書く。先回りしない。
2. **テスト** — ユニットテスト（`zig build test`）で純粋関数を固め、必要なら実機確認（`tcpdump` / `ping` / `nc`）を行う。
3. **コードリーディング** — 自分の書いたコードを読み直し、各ステップの「理解のポイント」に自分の言葉で答えられるか確認する。答えられない箇所が、次に RFC を読むべき場所。
4. **コミット** — 1 ステップ = 1 コミット。後から履歴を追うと、スタックが層ごとに育っていく過程がそのまま教材になる。

**テストデータの取り方**: パース系のテストに埋め込む実バイト列は、コンテナ内で `tcpdump -i tap0 -xx -c 1 arp`（プロトコル名は適宜変更）を実行して取得する。`-xx` は Ethernet ヘッダを含めた全バイトを表示する。

## この順序にした理由

- **チェックサムを最初に実装する。** IP / ICMP / UDP / TCP のすべてが同じアルゴリズムを使う。ここを先に押さえると、以降の各層で「なぜこのフィールドがここにあるのか」が読めるようになる。
- **ARP を IP より先にやる。** 「L3 アドレスから L2 アドレスを引く」という層の依存関係が、最も小さく分かりやすい形で現れる。しかも要求と応答が 1 往復で完結するので、最初に「自作スタックが応答を返す」体験ができる。
- **IP 単体では動作確認できない。** だから IP はパーサだけ作って dump で確認し、ICMP で初めて往復させる。この「L3 は単体では意味を持たない」という感覚自体が理解の一部。
- **UDP で疑似ヘッダを学ぶ。** L4 のチェックサムがなぜ L3 のアドレスを含むのか（＝誤配送の検知）を、TCP の複雑さに入る前に単純な UDP で理解しておく。
- **TCP は RST から入る。** コネクションが無いときの振る舞い（CLOSED の規則）は、状態機械の中で最も小さく、かつ即座に動作確認できる（`nc` が Connection refused になる）。「接続を張る」前に「接続を断る」を作る。
- **TCP は状態機械を先に、I/O は後に。** 遷移を純粋関数として書けば、ソケットも TAP も無しに全遷移をテストで網羅できる。TCP の難しさの本質は状態遷移であって I/O ではない。

---

## Phase 0 — 土台（ネットワーク不要）

### Step 1: hexdump とバイトオーダー変換

- **実装**: バイト列を `tcpdump -x` 風に 16 進ダンプする関数。`std.mem.readInt` / `writeInt` でビッグエンディアンの u16 / u32 を読み書きするヘルパ。
- **テスト**: 既知のバイト列 → 期待する文字列。`readInt(u16, &[_]u8{0x08, 0x00}, .big) == 0x0800`。
- **理解のポイント**: なぜネットワークバイトオーダーはビッグエンディアンなのか。x86 / ARM はリトルエンディアンなので、読み書きのたびに変換が要ることを体で覚える。
- **次への繋がり**: 以降すべてのデバッグはこの dump 関数越しに行う。

### Step 2: インターネットチェックサム（RFC 1071）

- **実装**: 16bit ワードの 1 の補数和の 1 の補数を返す関数。奇数長のパディング処理を忘れずに。
- **テスト**: RFC 1071 の例、および実際の IPv4 ヘッダのバイト列で「チェックサムフィールドを 0 にして計算した結果 == 元のチェックサム値」を確認する。
- **理解のポイント**: なぜ 1 の補数和なのか（エンディアン非依存、部分和の再利用が可能）。検証時に「フィールドを含めたまま計算すると 0 になる」性質はなぜ成り立つか。
- **次への繋がり**: Step 11（IP）、Step 13（ICMP）、Step 14（UDP）、Step 17（TCP）で同じ関数を使い回す。

---

## Phase 1 — パケットを目にする

### Step 3: TAP デバイスを開いて生フレームを dump

- **実装**: `/dev/net/tun` を open し、`TUNSETIFF` ioctl で `IFF_TAP | IFF_NO_PI` を指定して `tap0` に紐付ける。read したバイト列を Step 1 の dump にかけて標準出力へ。
- **ハマりどころ**:
  - `TUNSETIFF` や `IFF_TAP` / `IFF_NO_PI` の定数は Zig の標準ライブラリに無いので自分で定義する（`TUNSETIFF` は `_IOW('T', 202, c_int)` = `0x400454ca`）。`ifreq` 構造体は `std.os.linux` にあれば使い、無ければ定義する。
  - `IFF_NO_PI` を付けないと先頭に 4 バイトのパケット情報ヘッダが付いてズレる。
  - **スタックのプロセスが fd を開いている間だけ tap0 にキャリアが立つ。** プロセスが動いていないとカーネルはフレームを流してくれない。`ip link` で `NO-CARRIER` 表示を確認してみると分かる。
- **テスト**: スタックを起動した状態で `ping 192.168.70.2`（応答は無くてよい）を叩くと ARP 要求のフレームが流れてくる。`tcpdump -i tap0 -xx` の出力と自分の dump が一致することを確認する。
- **理解のポイント**: TAP から読めるのは「Ethernet フレームそのもの」であり、プリアンブルも FCS も含まれない。カーネルがどこまでやってくれているのかを意識する。
- **次への繋がり**: 以降のすべての入力はここから来る。

---

## Phase 2 — Ethernet (L2)

### Step 4: Ethernet ヘッダのパース

- **実装**: 先頭 14 バイトを宛先 MAC (6) / 送信元 MAC (6) / EtherType (2) に分解する純粋関数。ペイロードは入力スライスへの参照で返す。main のループに「EtherType で分岐する」骨組みを入れる（今は分岐先で dump するだけ）。
- **テスト**: Step 3 で拾った実フレームのバイト列を埋め込み、EtherType が `0x0806`（ARP）になることを確認。短すぎる入力でエラーを返すことも。
- **理解のポイント**: EtherType による上位層の多重分離。`0x0800`=IPv4、`0x0806`=ARP、`0x86DD`=IPv6。ブロードキャスト MAC `ff:ff:ff:ff:ff:ff` の意味。宛先が「自分の MAC でもブロードキャストでもない」フレームは無視してよいか（TAP では基本的に届かないが、実 NIC のプロミスキャスモードとの違いを考える）。
- **次への繋がり**: 以降の受信処理はすべて「EtherType で分岐」の形になる。

### Step 5: Ethernet フレームの組み立てと送信

- **実装**: 宛先 MAC・送信元 MAC・EtherType・ペイロードからフレームを組み立て、TAP に write する。スタック自身の MAC アドレス（CLAUDE.md「スタックの設定」参照）をここで定義する。
- **テスト**: 適当なフレームを送出し、`tcpdump -i tap0` に現れることを確認する（中身が不正でもよい。まず「送れる」ことの確認）。
- **理解のポイント**: 自分の MAC はなぜ `02:` で始めるのか（ローカル管理ビット）。最小フレーム長 60 バイト（FCS 除く）のパディングは誰がやるのか。TAP 経由では通常カーネル側が面倒を見るが、実 NIC ではどうか。
- **次への繋がり**: ここから上のすべての層が、最終的にこの関数を呼んで送信する。

---

## Phase 3 — ARP: 最初の往復

### Step 6: ARP パケットのパース

- **実装**: ハードウェア種別 / プロトコル種別 / アドレス長 / オペコード / 送信元 MAC・IP / 宛先 MAC・IP の 28 バイトを分解する。
- **テスト**: Step 3 で拾った実際の ARP 要求バイト列でフィールドを検証。
- **理解のポイント**: ARP ヘッダにアドレス長のフィールドがあるのはなぜか（ARP は Ethernet + IPv4 専用の仕様ではなく、汎用のアドレス解決プロトコルとして設計されている）。
- **次への繋がり**: 次のステップで応答を作る。

### Step 7: ARP 応答を返す 🏁 **マイルストーン 1**

- **実装**: 自分の IP（`192.168.70.2`）宛の ARP 要求を受けたら、オペコードを reply にして送信元・宛先を入れ替えた応答を Step 5 で送出する。
- **テスト**: コンテナ内で `ip neigh flush all && ping -c1 192.168.70.2` を実行し、`ip neigh` に自分のスタックの MAC が `REACHABLE` として現れること。
- **理解のポイント**: 相手（Linux カーネル）はまだ ping には応答が返っていないのに、ARP のレベルでは会話が成立している。層が独立して動いていることを実感する。
- **次への繋がり**: **自作スタックが初めて外の世界に応答した瞬間。** ここから先は「受けて返す」の繰り返し。

### Step 8: ARP テーブルと ARP 要求

- **実装**: IP → MAC のキャッシュ（固定長配列で十分）。受信した ARP から学習してテーブルに載せる。そして「この IP の MAC を教えろ」と自分から ARP 要求をブロードキャストする関数。
- **テスト**: テーブルの挿入・検索・上書きをユニットテストで。実機では `192.168.70.1` への ARP 要求を送ると、カーネルが応答を返してきてテーブルに載ることを `tcpdump` と合わせて確認する。
- **理解のポイント**: ARP 要求の「宛先 MAC」フィールドには何を入れるのか（Ethernet ヘッダはブロードキャスト、ARP 本体の target MAC は 0 埋め — 2 箇所あることに注意）。エントリの有効期限は必要か。
- **次への繋がり**: 次のステップでこのテーブルを送信経路に組み込む。

### Step 9: 送信経路の共通化

- **実装**: 「IP アドレス宛にペイロードを送る」共通関数。テーブル検索 → 未解決なら ARP 要求を送って保留 → 解決済みなら Ethernet 送信、という流れ。
- **テスト**: 解決済み IP への送信が即座に Ethernet フレームになること、未解決 IP への送信で ARP 要求が先に出ることをユニットテスト（TAP の write をモックするか、送信バッファを注入して確認）。
- **理解のポイント**: 未解決の間、送りたかったパケットをどうするか（捨てる / 1 個だけ保持する / キューに積む）。実装によって振る舞いが違う設計判断ポイント。Linux は何個キューイングするか調べてみる。
- **次への繋がり**: ICMP / UDP / TCP の送信は、すべてこの共通関数を通る。ここを作っておくと以降が楽になる。

---

## Phase 4 — IPv4 (L3)

### Step 10: IPv4 ヘッダのパース + チェックサム検証

- **実装**: バージョン / IHL / TOS / 全長 / 識別子 / フラグ / フラグメントオフセット / TTL / プロトコル番号 / チェックサム / 送信元・宛先 IP。Step 2 でチェックサムを検証し、不正なら破棄。宛先が自分の IP でないものも破棄。
- **テスト**: 実際の ICMP echo request のバイト列を埋め込み、プロトコル番号が 1、チェックサム検証が通ることを確認。1 バイト壊したら検証が落ちることも。
- **理解のポイント**: IHL がバイト数ではなく 32bit ワード数なのはなぜか。チェックサムがヘッダのみを対象としペイロードを含まないのはなぜか（ルータが TTL を減らすたびに再計算する必要があるため）。「全長」フィールドと Ethernet フレーム長の関係 — 最小フレーム長のパディングを剥がすのは誰か。
- **次への繋がり**: プロトコル番号（1=ICMP、6=TCP、17=UDP）による多重分離が、Step 4 の EtherType と同じ構造であることに注目。

### Step 11: IPv4 ヘッダの組み立てと送信

- **実装**: ペイロードとプロトコル番号・宛先 IP からヘッダを組み立て、チェックサムを計算して Step 9 経由で送信する。
- **テスト**: 送出したパケットを `tcpdump -i tap0 -vv` で確認。`bad cksum` と表示されないこと（tcpdump はチェックサムを検証してくれる。強力なデバッグ手段）。
- **理解のポイント**: TTL の初期値は何にすべきか。識別子フィールドは何に使われるか（フラグメント再構成）。フラグメントは今は実装せず、`DF` を立てるか未実装のまま進めるかを決める。
- **次への繋がり**: IP は単体では何も応答しない。次の ICMP で初めて往復する。

---

## Phase 5 — ICMP: ping が通る

### Step 12: ICMP Echo のパース

- **実装**: タイプ / コード / チェックサム / 識別子 / シーケンス番号 + データ。ICMP のチェックサムは ICMP メッセージ全体が対象（疑似ヘッダは無い）。
- **テスト**: 実際の echo request バイト列で、タイプ 8、チェックサム検証が通ることを確認。
- **理解のポイント**: ICMP は IP の上に乗るが「L4」とは呼ばれない。ポート番号を持たないことに注目。識別子とシーケンス番号は誰が何のために使うか（`ping` プロセスが自分宛の応答を識別するため）。

### Step 13: Echo Reply を返す 🏁 **マイルストーン 2**

- **実装**: タイプを 8→0 にし、データをそのままコピーしてチェックサムを再計算、Step 11 で送り返す。
- **テスト**: コンテナ内で `ping -c3 192.168.70.2` が **応答を受け取る**。RTT が表示され、パケットロス 0%。
- **理解のポイント**: この瞬間に TAP → Ethernet → ARP → IPv4 → ICMP の経路が全部つながっている。1 回の ping で自分のコードのどこを何回通ったか、追ってみる。
- **次への繋がり**: 「往復する経路」が完成した。以降はこの経路の上に L4 を積む。

---

## Phase 6 — UDP: L4 の入り口

### Step 14: UDP ヘッダと疑似ヘッダチェックサム

- **実装**: 送信元ポート / 宛先ポート / 長さ / チェックサム。チェックサムは **疑似ヘッダ**（送信元 IP・宛先 IP・0・プロトコル番号・UDP 長）+ UDP ヘッダ + データを対象に計算する。
- **ハマりどころ**:
  - IPv4 では UDP チェックサム 0 は「未計算」を意味する。検証をスキップする分岐が要る。
  - カーネルがチェックサムオフロードを前提にして、L4 チェックサム未計算のままフレームを流してくる場合がある。受信検証が謎に失敗するときは `ethtool -K tap0 tx off` でオフロードを無効化して切り分ける。
- **テスト**: 実際の UDP パケットのバイト列で検証が通ること。疑似ヘッダの組み立てを別関数にして単独でもテストする。
- **理解のポイント**: **なぜ L4 のチェックサムが L3 のアドレスを含むのか。** 答えは「誤配送の検知」。IP ヘッダが壊れて別ホストに届いた場合、L4 チェックサムが合わないので気付ける。層を厳密に分離していない、実用上の妥協点。
- **次への繋がり**: TCP のチェックサムもまったく同じ疑似ヘッダを使う。ここで作った関数は TCP でも使い回せる。

### Step 15: UDP echo サーバ 🏁 **マイルストーン 3**

- **実装**: 特定ポート宛の UDP を受けたら、送信元と宛先を入れ替えて同じデータを返す。
- **テスト**: `echo hello | nc -u 192.168.70.2 7` で `hello` が返ってくる。
- **理解のポイント**: ICMP echo との違いはポート番号だけ、とも言える。UDP が IP に対して足したものは実質「ポート番号」と「オプションのチェックサム」だけだと確認する。

### Step 16: ポート多重化（ソケットテーブル）

- **実装**: 「ポート番号 → ハンドラ」のテーブル。受信した UDP を宛先ポートで振り分け、未使用ポートなら ICMP Port Unreachable を返す（余裕があれば）。
- **テスト**: 2 つの異なるポートに別々のハンドラを登録し、それぞれ正しく振り分けられることをユニットテストで。
- **理解のポイント**: これが「ソケット」の正体。OS のソケット API が裏で何をしているかが見える。宛先ポートだけで振り分けられる UDP に対し、TCP は何で識別する必要があるか（4 タプル）を先に考えておく。
- **次への繋がり**: TCP のコネクション管理は、このテーブルの拡張版になる。

---

## Phase 7 — TCP

ここからが本番。TCP は「信頼性の無い層の上に信頼性を作る」仕組みの塊で、状態機械・順序制御・再送・フロー制御が絡み合う。**一度に全部やらず、必ず段階を分けること。**

### Step 17: TCP ヘッダのパース

- **実装**: 送信元・宛先ポート / シーケンス番号 / ACK 番号 / データオフセット / フラグ (URG,ACK,PSH,RST,SYN,FIN) / ウィンドウ / チェックサム / 緊急ポインタ。オプションは長さだけ読んで読み飛ばしてよい。
- **テスト**: 実際の SYN パケットのバイト列で SYN フラグが立っていること、疑似ヘッダ込みのチェックサム（Step 14 の関数を再利用）が通ることを確認。
- **理解のポイント**: データオフセットがあるのは可変長オプションのため。ウィンドウフィールドが最初から存在することに注目 — フロー制御は TCP の後付けではなく設計の中核。

### Step 18: RST 応答（CLOSED の振る舞い）

- **実装**: コネクションを 1 つも持たない状態で、届いた TCP セグメントに RST を返す。RFC 9293 3.10.7.1 の規則どおり: 受信セグメントに ACK があれば `seq = 受信 ACK 番号`、無ければ `seq = 0, ack = 受信 seq + セグメント長（SYN/FIN は +1）` で RST+ACK。
- **テスト**: `nc 192.168.70.2 9999` が即座に **Connection refused** になる。`tcpdump -i tap0 -S` で SYN → RST の 2 パケットを確認。
- **理解のポイント**: なぜ RST にも正しいシーケンス番号が要るのか（相手は番号が合わない RST を無視する — 盲目的リセット攻撃への防御）。「ポートが閉じている」が UDP では ICMP、TCP では RST で伝わる違い。
- **次への繋がり**: これが状態機械の CLOSED 状態の規則そのもの。次のステップで残りの状態を作る。

### Step 19: 状態機械を純粋関数として実装

- **実装**: 状態の enum（CLOSED / LISTEN / SYN_SENT / SYN_RECEIVED / ESTABLISHED / FIN_WAIT_1 / FIN_WAIT_2 / CLOSE_WAIT / CLOSING / LAST_ACK / TIME_WAIT）と、`fn transition(state, event) -> (next_state, action)` の形の**純粋関数**。I/O は一切書かない。まず handshake に関わる遷移（LISTEN → SYN_RECEIVED → ESTABLISHED）から書き、クローズ系遷移を足していく。
- **テスト**: **ここが最重要。** RFC 9293 の状態遷移図を見ながら、全遷移をユニットテストで網羅する。3-way handshake、同時オープン、同時クローズ、RST 受信。TAP もソケットも不要で全部テストできる。
- **理解のポイント**: なぜ CLOSE_WAIT と FIN_WAIT_2 が非対称に見えるのか。なぜ TIME_WAIT だけ時間で抜けるのか。図を見ずに書けるようになるまで繰り返す。
- **次への繋がり**: 以降のステップは、この純粋な状態機械に I/O とバッファを接続していく作業。

### Step 20: 3-way handshake（受動オープン） 🏁 **マイルストーン 4**

- **実装**: 4 タプル（送信元 IP・ポート / 宛先 IP・ポート）をキーにした TCB（Transmission Control Block）テーブル。LISTEN 状態で SYN を受けたら SYN+ACK を返し、ACK を受けて ESTABLISHED へ。初期シーケンス番号の生成。TCB が見つからないセグメントは Step 18 の RST に落ちる。
- **テスト**: `nc 192.168.70.2 8080` が接続を確立する（プロンプトが返ってこず待機状態になる）。`nc 192.168.70.2 9999` は引き続き Connection refused になる（listen していないポート）。`tcpdump -i tap0 -S` で 3 パケットのやり取りを目視確認。
- **理解のポイント**: なぜ 2-way ではなく 3-way なのか。ISN をランダムにする理由。SYN に対する ACK 番号が `seq+1` になるのはなぜか（SYN はシーケンス空間を 1 消費する）。
- **次への繋がり**: コネクションという「状態」が生まれた。以降はこの状態にデータの流れを載せる。

### Step 21: データ受信と ACK

- **実装**: 受信バッファ。シーケンス番号の順序チェック、順序どおりなら受け入れて ACK、重複は破棄、順序が飛んだら（まずは）破棄。
- **テスト**: `nc` から文字列を送って受信できること。`tcpdump` で ACK 番号が正しく進むこと。ユニットテストではシーケンス比較関数（ラップアラウンド対応）を重点的に。
- **理解のポイント**: シーケンス番号のラップアラウンド（32bit で一周する）をどう比較するか。単純な `<` では破綻する — 符号付き差分で比較する必要がある。ここは実装で必ずバグる場所。

### Step 22: データ送信と再送

- **実装**: 送信バッファと未 ACK データの保持。タイマで一定時間 ACK が来なければ再送。まずは固定タイムアウトで十分。**イベントループにここで初めて「時間」が入る** — ブロッキング read を poll(timeout) に変え、タイムアウトでタイマ処理を回す。
- **テスト**: 送ったデータが `nc` 側に表示される。意図的に ACK を無視するテストで再送が起きることを確認。
- **理解のポイント**: 「ACK が返るまでデータを捨てられない」というのが信頼性の本質。RTO を固定値にすると何が起きるか（RFC 6298 の RTT 推定へ繋がる動機を体感する）。

### Step 23: クローズ処理と TIME_WAIT

- **実装**: FIN の送受信、Step 19 の状態機械に沿った遷移。TIME_WAIT の 2MSL タイマ（Step 22 のタイマ機構を再利用）。
- **テスト**: `nc` を Ctrl-D で閉じたときに正しく 4-way でクローズすること。`tcpdump` で FIN / ACK / FIN / ACK を確認。
- **理解のポイント**: **なぜ TIME_WAIT が必要か。** 遅延した重複パケットが次のコネクションに混入するのを防ぐため、および最終 ACK が失われた場合に再送に応じるため。サーバで `TIME_WAIT` が大量に溜まる現実の運用問題が、ここで腑に落ちる。

### Step 24: ウィンドウとフロー制御

- **実装**: 受信バッファの空き容量を advertised window としてヘッダに載せる。相手のウィンドウを超えて送らない。ウィンドウ 0 のときのプローブ。
- **テスト**: 受信側（自作スタック）でバッファを消費せずにいると、カーネル側の送信が止まることを `tcpdump` で確認する。
- **理解のポイント**: フロー制御（相手の受信能力に合わせる）と輻輳制御（ネットワークの容量に合わせる）は別物であることを区別する。Silly Window Syndrome とは何か。

---

## 発展課題（Phase 7 完了後）

- **能動オープン**（クライアント側）— SYN_SENT の経路。自作スタックから外に接続する。
- **輻輳制御** — スロースタート、輻輳回避、高速再送（RFC 5681）。
- **TCP オプション** — MSS、ウィンドウスケール、SACK、タイムスタンプ。
- **順序が乱れたセグメントの再構成** — Step 21 で破棄していたものを保持して並べ替える。
- **IP フラグメント** — 分割と再構成。
- **ICMP のエラー通知** — Destination Unreachable / Time Exceeded。TTL を減らして転送すれば `traceroute` の対象にもなる。
- **簡易ソケット API** — `listen` / `accept` / `read` / `write` を自作スタックの上に実装し、既存のプログラムを載せてみる。
