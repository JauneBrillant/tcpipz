# tcpipz

Zig で TCP/IP プロトコルスタックをスクラッチから自作する学習プロジェクト。
OS のソケット API に頼らず、Ethernet から TCP まで自前で実装する。

## 環境

- Zig **0.16.0**（最新安定版）。ホストでは zvm で管理（`zvm use 0.16.0`）。
- 実行環境は **Docker 上の Linux コンテナ**。パケット送受信には **TAP デバイス**（L2、Ethernet フレーム単位の I/O）を使う。コンテナには `NET_ADMIN` ケーパビリティと `/dev/net/tun` が必要（compose.yaml で設定済み）。
- ホストは macOS (Apple Silicon)。エディタ補完用にホストにも Zig を入れているが、ビルド・実行・動作確認はコンテナ内で行うのが基本。

## コマンド

```sh
# コンテナ起動・シェルに入る
docker compose up -d
docker compose exec dev bash

# コンテナ内で
zig build          # ビルド
zig build run      # 実行
zig build test     # 全テスト実行
zig fmt src/       # フォーマット
```

tap0 は `entrypoint.sh` がコンテナ起動時に用意する（作成・IP 付与・UP・チェックサムオフロード無効化）。手動での設定は不要。

注意: tap0 にキャリアが立つのは**スタックのプロセスが fd を開いている間だけ**。スタックを起動していない状態では、カーネルは tap0 にフレームを流さない（`ip link` で `NO-CARRIER` と表示される）。

## スタックの設定

自作スタック側の固定値。ハードコードでよい。

| 項目 | 値 |
|---|---|
| スタックの IP | `192.168.70.2` |
| スタックの MAC | `02:00:00:00:00:02`（ローカル管理アドレス） |
| カーネル側（対向） | `192.168.70.1`（tap0 に付与） |

## アーキテクチャ方針

レイヤごとにモジュールを分ける（下から順に実装する予定）：

```
src/
  main.zig      # エントリポイント（TAP のセットアップ、イベントループ）
  root.zig      # ライブラリのルート
  hexdump.zig   # 16 進ダンプ・バイトオーダー変換ヘルパ
  checksum.zig  # インターネットチェックサム + 疑似ヘッダ (RFC 1071)
  Tap.zig       # TAP デバイスの open / read / write
  ethernet.zig  # Ethernet II フレームの解析・構築
  arp.zig       # ARP: リクエスト応答とテーブル管理 (RFC 826)
  ip.zig        # IPv4: ヘッダ解析・チェックサム・フラグメント (RFC 791)
  icmp.zig      # ICMP: Echo Reply (RFC 792)
  udp.zig       # UDP (RFC 768)
  socket.zig    # ポート多重化テーブル
  tcp.zig       # TCP: ステートマシン、再送、フロー制御 (RFC 793 / RFC 9293)
```

- 各レイヤは **バイト列のパース／シリアライズを純粋関数として実装**し、I/O から分離する。これによりパケットの実データを使ったユニットテストが書きやすくなる。
- ヘッダ構造体はネットワークバイトオーダー（ビッグエンディアン）との変換を明示的に行う。`std.mem.readInt` / `writeInt` を使い、`packed struct` のエンディアン依存には頼らない。
- アロケーションは受信バッファの管理箇所に限定し、パース処理はゼロコピー（入力スライスへの参照）を基本とする。

## テスト方針

- 各モジュールに `test` ブロックを併置する（Zig の慣習どおり）。
- パース系のテストは、実キャプチャ由来のバイト列をテストデータとして埋め込んで検証する。バイト列は `tcpdump -i tap0 -xx -c 1 <filter>` で取得する。ユニットテストは TAP 不要なのでホストでも実行できる。
- 動作確認にはコンテナ内の `tcpdump -i tap0` / `ping` / `nc` を使う。`tcpdump -vv` はチェックサムを検証してくれるのでデバッグに有効。

## 実装の進め方

学習目的のため、**スモールステップで進める**。各ステップは「動作確認できる最小の実装 → テスト → コードリーディング → コミット」の 4 段階で完了とし、先回りした実装はしない。1 ステップ = 1 コミットで、履歴自体を教材にする。コードリーディングでは「理解のポイント」に自分の言葉で答える — 答えられない箇所が、次に RFC を読むべき場所。

ステップの一覧・順序・各ステップの理解のポイントは **[ROADMAP.md](ROADMAP.md)** にある。作業を始める前にそこの「進捗」チェックリストで現在のステップを確認し、ステップ完了時にチェックを付けること。

マイルストーン: ① ARP 応答（Step 7）→ ② ping 応答（Step 12）→ ③ UDP echo（Step 14）→ ④ TCP 3-way handshake（Step 19）。

## 参照 RFC

- RFC 826 (ARP) / RFC 791 (IPv4) / RFC 792 (ICMP) / RFC 768 (UDP) / RFC 9293 (TCP)
- チェックサム計算は RFC 1071

## 規約

- コメント・ドキュメントは日本語で書いてよい。識別子は英語。
- ファイル名: 型 1 つのファイルは大文字始まりの file-as-struct（`Tap.zig`）、関数の集まりは小文字（`ethernet.zig`）。
- コミット前に `zig fmt` を通すこと。
